# 仕様

壁打ちで決まったことを置く場所。決まっていないものは「保留」に残す。

## なぜ作るのか

CloudMounter で Google Drive を外付けにマウントしていたところ、ドライブの一番上に
自分が作った覚えのないファイルが並び、案件が増えるたびに増え続けていた。

正体は、リンク共有で開いたことのある他人のファイル。ブラウザのマイドライブにも
共有アイテムにも出ないのに、CloudMounter だけがルートに並べていた。CloudMounter 側に
表示を絞る設定は無く（アプリの文言リソースを全部確認した）、Drive 側にも消せる実体が無い。

同じことが何度も起きているので、道具を替える。

## 決まったこと

### 名前

**Gocci（ゴッチ）。**

- 既存の macOS アプリでは見つからなかった。出てきたのはミュージシャン数名、
  大学のキャンパスアプリ goCI（綴りが違う）、活動の止まった Gocci Software の X アカウント、
  アルバニア系の姓
- **Gucci と1文字違いであることは承知のうえで採る。** 商標は指定区分が違えば衝突しにくく、
  ファッションブランドとメニューバーアプリが実際に問題になることはまず無い。
  実害は検索が混ざる程度
- 見送った候補と理由
  - Kumochi — 「雲を持つ」で機能とまっすぐ繋がるが、macOS に Kumo という別アプリ（プロキシ）がある
  - Tsunachi — Twitch に同名の配信者、App Store に語感の近い Tsunagi がある（Konechi のときと同じ理由）
  - Kumobako — 4音でやや長い

名前が機能を説明しないぶん、**アイコンが意味を引き受ける**。

### 対応範囲

**Google Drive 専用。** 他のリモートには広げない。

汎用の rclone GUI は RcloneView、Rclone UI、Rclone Manager、RcloneBrowser と既に4本あり、
公式にも Web GUI がある。後発で対応数を競っても不利。

一方「Google Drive を macFUSE 無しで、好きな場所に、共有アイテム抜きでマウントする」は
空白だった。公式アプリはマウント先を選べず、Mountain Duck も macOS では内蔵ディスク固定、
CloudMounter は共有アイテムが散らかる。

後から他のリモートを足すのは技術的には簡単なので、広げる余地は残る。

### マウント方式

**rclone の NFS 方式（`rclone nfsmount`）。macFUSE は使わない。**

| 方式 | 導入の手間 | マウント先 | 状態 |
| --- | --- | --- | --- |
| NFS（rclone 内蔵） | なし | 自由 | rclone 側が Experimental |
| macFUSE | カーネル拡張＋再起動＋許可 | 自由 | 枯れている |
| File Provider（OS標準） | なし | `~/Library/CloudStorage` 固定 | 安定 |

導入の手間がゼロで、かつマウント先を自由に選べるのはこの方式だけ。
外付けに置けることが今回の出発点なので、ここは譲れない。

背負うリスク:

- rclone が [serve nfs](https://rclone.org/commands/rclone_serve_nfs/) を **Experimental** と明記している
- NFS サーバーに認証が無い（既定で localhost の乱数ポートに閉じるので実害は小さい）
- 書き込みには `--vfs-cache-mode full` が必須。指定しないと読み取り専用になる

前例として macsh が同じ方式で動いている（Google Drive 非対応なだけ）。

macFUSE を採らなかったのは、ここで脱落する人が多すぎるため。
CloudMounter も Mountain Duck も File Provider に移っているのは、たぶん同じ判断。

**File Provider でも外付けに置ける。ここは 2026-08-13 に訂正した。**

前日に「置き場所は `~/Library/CloudStorage` 固定」と書いたが、誤り。
`NSFileProviderDomain(displayName:userInfo:volumeURL:)`（macOS 15 以上）でボリュームを
指定できる。CloudMounter は実際にこれで外付けに置いていた（アプリのバイナリに同じ API と
`NSFileProviderExternalVolumeHandling` があり、`/Volumes/HIKSEMI/.CloudStorage/Data/…` に
OS 管理の領域が作られている）。

**採れない理由は別にある。アドホック署名では File Provider のドメインを登録できない。**
手元で確かめた（2026-08-13）:

| 試したこと | 結果 |
| --- | --- |
| 外付けを指定して登録 | 失敗（-2001 提供元が見つからない） |
| 置き場所を指定せず登録 | 同じ失敗。外付けの問題ではない |
| 拡張を pluginkit に登録 | 一覧には載る。それでも失敗 |
| アプリと拡張の両方に App Group と sandbox を付与 | 同じ失敗 |

`fileproviderd` が `getDomainsForProviderIdentifier((null))` を返す。システムがアプリを
提供元として識別できていない。App Group は「Team ID＋名前」の形で、Developer Program に
登録した開発者にしか作れない（CloudMounter は `XS85JU6YZ3.com.eltima.cloudmounter`）。

**つまり File Provider へ移るには年 99 USD が要る。** 払えば、外付けに置いたまま
`dataless`（覗いても落ちてこない）と macOS 標準の雲バッジが手に入る。
払わないなら NFS 方式のままで、Finder が中を読むぶんは落ちてくる。

---

**2026-08-16、これを覆して File Provider へ移した。上の「年 99 USD が要る」は誤り。**

もう一度、空の拡張を作って確かめた。アドホック署名・団体識別子なし・**App Group なし**で
ドメインの登録まで通った。`~/Library/CloudStorage/FPTest-FPTest` が Finder に現れた。
拡張に要ったのは `com.apple.security.app-sandbox` だけ。

前回 `-2001` で失敗していたのは、署名のせいではなかった。**ドメインを足す側が違った。**
`NSFileProviderManager.add` は拡張を含むアプリ本体から呼ぶ必要がある。前回はコマンドラインの
別バイナリから呼んでいて、システムから見れば提供元が存在しないので `-2001` が返る。
アプリ本体に組み込んで呼んだら通った。

手元の4つ（CloudMounter・iCloud Drive・写真）が全部 App Group を持っていたのは事実だが、
それは「そうしている」であって「必須」ではなかった。実物を見て必須だと決めつけていた。

移った理由は、NFS 方式のままでは解けない問題が残るため。Finder から見て普通のディスクである
以上、アイコンのプレビューのために中を読まれる。それを止める手立てが `.DS_Store` を
書いて回ることしかなく、その回避策が 2026-08-15〜16 に次の障害を生んだ:

- 掃引が3628個の `.DS_Store` を書き、rclone の起動時の数え上げが57秒に伸びて、
  マウントが120秒の見切りに届かなくなった
- 掃引の途中で rclone が落ちると、空のフォルダと読み違えて待ち行列を捨て、
  「歩き終えた」と嘘の記録を残した
- `statfs` を主スレッドで呼んでいて、応答の無いマウントでメニューが開かなくなった
- 起動処理が `getxattr` で固まり、アプリごと立ち上がらなくなった

File Provider ではこれらが起きない。サムネイルは専用の口（`fetchThumbnails`）で訊かれ、
本体は読まれない。途中だけ渡す道（`NSFileProviderPartialContentFetching`）もある。
71.8MB のファイルから 1KB 読んで、実際に取ったのは 16KB だけだった（実測）。

引き換えに、外付けを指定する道は落とした。`volumeURL` で置けるはずだが確かめていない。
今の作りでは置き場所を macOS に任せている。

### 実装で裏を取った内容（2026-08-12）

rclone v1.75.0 / macOS 26.5.2 / 外付け APFS（USB）で、実際に確かめた。

```bash
rclone nfsmount gdrive: /Volumes/HIKSEMI/GocciTest \
  --vfs-cache-mode full \
  --cache-dir /Volumes/HIKSEMI/.gocci-cache \
  --drive-export-formats webloc \
  --daemon
```

| 確かめたこと | 結果 |
| --- | --- |
| macFUSE 無しでマウント | 可。`localhost:/ on … (nfs, nodev, nosuid, mounted by piro)` |
| 外付けに置ける | 可 |
| 共有アイテムが出ない | 出ない。17フォルダ＋1ファイルで、ブラウザのマイドライブと完全一致 |
| 読み書き | 可。新規作成・追記・削除とも Drive に反映（58バイト、2行とも到達） |
| Google ドキュメント類 | `.webloc` で出る。開くとブラウザで Drive が開く |
| 速度 | フォルダ一覧が 0.6 秒 |

`--drive-shared-with-me` の既定は `false`。**何もしなくても共有アイテムは出ない。**

### client_id は利用者が用意する

rclone が共用している Google の client_id は **2026年中に停止する**（rclone 自身が起動時に警告を出す）。
各自で Google Cloud Console から作る必要がある。

配布するアプリが自前の client_id を同梱する道は採らない。本番公開のアプリが制限付きスコープを
使う場合、ブランド審査に2〜3営業日、データアクセス審査に数週間、第三者によるセキュリティ評価
（CASA）が要ることがある。審査を受けず「テスト」状態のまま出すと、利用者数の上限・警告画面・
リフレッシュトークンの寿命制限が付き、配布物として成り立たない。

**Gocci が持つのは、client_id と client_secret の入力欄と、取得手順の案内まで。**
Cloud Console を開くリンクを添える。凝った案内は作らない。基本は自分が使う前提。

実装した（2026-08-12）。値は UserDefaults に持たない。持ち主は rclone の設定ファイルで、
二重に持てば必ずずれる。読み書きは `rclone config show` と `rclone config update` に頼む。

**client_id を変えたら認証をやり直す必要がある。** 変えた時点で今の認証は無効になるので、
書き込みだけでは繋がらなくなる。設定画面の「保存して認証し直す」は、書き込み →
`rclone config reconnect`（ブラウザで同意）→ 繋ぎ直し、までを続けて行う。

### 最初のバージョンに入れるもの

- Google アカウントを1つ繋ぐ
- マウント先を選ぶ（外付けを選べる）
- メニューバーからマウント / アンマウント
- 共有アイテムを表示しない
- ログイン時に自動マウント。外付けが繋がるまで待つ

### 入れないもの

- 複数アカウント
- 共有ドライブ、共有アイテムの表示切り替え
- キャッシュ容量の管理画面
- Google Drive 以外のサービス
- 同期モード（常にストリーミングのみ）

狙いは、CloudMounter を今日そのまま置き換えられる最小の形。

### メニューを開いたときに出すもの

```text
┌──────────────────────────────────┐
│ ● Google Drive          接続中   │
│   /Volumes/HIKSEMI/GoogleDrive   │
├──────────────────────────────────┤
│   Finder で開く                   │
│   接続を切る                      │
├──────────────────────────────────┤
│   設定…                           │
│   終了                            │
└──────────────────────────────────┘
```

手動の切り替えが要る理由は2つ。**外付けを抜くとき**（繋がったまま抜くとエラーになる）と、
**調子が悪いとき**（切って繋ぎ直せば直る）。普段はログイン時に自動で繋がるので、
このメニューを触るのは外付けを抜き差しするときくらい。

### rclone は同梱する

**アプリに同梱する。初回起動時に取りに行く方式は採らない。**

- **バージョンを握れる。** Experimental と明記された NFS 機能に乗るので、rclone 側の更新で
  挙動が変わると直撃する。こちらが検証したバージョンで固定する
- **初回起動の失敗要因を減らせる。** アドホック署名の警告をシステム設定で乗り越えてもらった
  直後に「rclone のダウンロードに失敗しました」が出るのは体験として最悪

代償は容量。rclone は MIT ライセンスで同梱に支障は無いが、macOS arm64 の配布物が 31MB あり、
Sparkle は差分更新をしないので、更新のたびにその全部を落とし直すことになる。
Konechi の DMG が 9.6MB なのに対し、Gocci は 40MB 前後になる見込み。

### 対応環境

Konechi に倣う。

- **arm64 のみ。** Intel は切る
- **macOS 14 以上**
- `LSUIElement` で Dock に出さず、メニューバーだけに常駐

### 技術スタック

**Swift / SwiftUI。** Xcode は使わず、Command Line Tools の `swiftc` と `build.sh`。

作るものはメニューバー常駐のユーティリティで画面はほとんど無い。ログイン項目への登録、通知、
rclone プロセスの起動と監視、署名、自動更新と、全部 macOS 側の作法に乗る話なのでネイティブが素直。
Tauri だと WebView を1枚抱えることになり、この規模には重い。

### 配布

Konechi と同じ形。

- **アドホック署名**（`codesign --sign -`）。Developer ID も公証も無し
- Apple Developer Program（年 99 USD）には登録しない
- 初回は「開発元未確認」の警告が出る。システム設定から許可してもらう。README に明記する
- 初回は DMG、更新は ZIP
- `release.sh` から `gh release create` で GitHub Releases へ
- LP は `lp/` に同居させて Next.js + next-intl。`gocci.kkweb.io`

Mac App Store には出さない。サンドボックスが必須で、外部プロセス（rclone）の起動と
任意の場所へのマウントが通らない。手元の CloudMounter を調べたところ、直配布版で
App Sandbox の entitlement を持っていなかった。同じ構成を採る。

### 自動更新

Sparkle。鍵はログインキーチェーン。`generate_appcast` で appcast.xml を作る。
Konechi の `release.sh` をそのまま持ってくる。

## 保留

### アイコン

**Galopen 方向。生き物にはしない。**
角丸スクエアにグラデーション背景と白いモチーフ、メニューバーは単色のシルエット。
Konechi のような表情で状態を表すキャラは採らない。

モチーフの候補として **雲 + ディスク**（雲の下半分が円盤）を挙げてある。
塊同士の組み合わせなので 18px でも潰れず、「クラウドをドライブとして扱う」がそのまま形になる。
状態は塗り・中抜き・薄い塗りで出し分け、エラーのときだけ小さいバッジを足す。

配色は Galopen が青紫のグラデーションなので、被らないように振る。

**ただし、見た目は後で決める。**

### 決めていないこと

- 落ちたことを通知で報せるか（今は状態表示だけ）
- 設定画面の項目

### フォルダを開いてもダウンロードしない（既定で入り、切り替えも残す）

Finder はサムネイルのために中を読む。読まれれば落ちてくる。フォルダのアイコンプレビューを
切れば読まれないが、**rclone は既定で `.DS_Store` を捨てる**（`--noappledouble` が既定で有効）ので、
切っても開き直すたびに元へ戻っていた。

`--noappledouble=false` を渡すと設定が残る。実機で確かめた（2026-08-13）:

| フォルダ | プレビュー | 開いたときの取得量 |
| --- | --- | --- |
| Movies | 切（設定が残る） | 0MB |
| Images | 有効 | 14MB |

代償は Drive 側に `.DS_Store` が増えること。**既定では入れる。**

切り替えは残してある。これは利用者の Drive にこちらがファイルを書く設定で、共有フォルダなら
相手にも見える。使う人が少なくても、断る手立てごと無くすのは避ける。

なお、ここに辿り着くまでに「フォルダごとの設定は保存できない」と誤って断定した。
`--noappledouble` は `--help` に最初から載っていた。読み切る前に結論を書いたのが原因。

#### 設定を入れた時点で、下のフォルダを全部切る

保存できるだけでは、一つずつ手で切って回ることになる。設定を入れた瞬間に、マウント先の
下を歩いて `.DS_Store` を置く（`Sources/FinderView.swift`）。

Finder に頼む道は無い。AppleScript の辞書では、表示オプションは Finder の**ウィンドウ**にしか
付いておらず、フォルダには付いていない（`Finder.sdef` 332-333 行）。フォルダを指定して
設定しようとすると `-10006` で断られる。フォルダの数だけウィンドウを開かせるのは現実的でない。

#### `.DS_Store` の形式（2026-08-14 に実物を解いて確かめた）

- **フォルダの表示設定は、そのフォルダの中ではなく一つ上の `.DS_Store` に、フォルダ名を鍵にして入る。**
  `Movies/.DS_Store` は存在せず、記録は `GoogleDrive/.DS_Store` にあった。
  自分自身の設定は `.` という名前で入る（ボリュームの根で確認）
- 容れ物は「バディアロケータ」。先頭に `Bud1`、ブロック表、目録（`DSDB`）、その先に名前で並べた木
- 節（ページ）の大きさは容れ物側に書いてあり、既定の 4KB より大きくても Finder は読む。
  301 フォルダ・1MB のファイルで確認。段を作らずに済むので、こちらは常に葉ひとつで書く
- 効かせるには鍵をひと通り入れる必要がある。`showIconPreview` だけの plist では**無視された**。
  `icvp`（アイコン表示）と `lsvp`（リスト表示）を一式で入れると効く。1フォルダあたり約 1.2KB
- 読めなかったファイルには触らない。書き直して壊すと、利用者が積み上げた設定を失う
- 既に `icvp` か `lsvp` を持つフォルダは飛ばす。その人が自分で決めた設定を上書きしない

実物（25 記録・18KB）を読んで書き直し、記録が一つも欠けないこと、Finder がそれを読んで
元の設定通りに振る舞うことを確かめてから、Drive へ適用した。

### 再生を止めても最後まで取得する（常に有効）

rclone は開かれた部分しか落とさない。動画を途中まで再生して止めれば、そこまでの範囲しか
手元に残らず、バッジもその割合で止まる。不具合ではなく、そういう作りになっている。

最初は rclone の先読み（`--vfs-read-ahead`）を有効にする形で作ったが、**これは筋が悪い。**
先読みは「読まれたファイル」に対して働くので、Finder がサムネイルのために覗いただけの
動画まで丸ごと落ちてくる。フォルダを眺めただけで数 GB 動く。「開いた」の範囲が
人の意図と合わない。

**rclone の先読みは使わない。最後まで取りにいく役目はアプリが持つ。**

- 素の rclone は、読んだところだけを落とす。フォルダを眺めても、サムネイルに要る分で済む
- アプリが**ある程度読まれたファイル**の続きを取りにいく。設定画面には出さない。
  開いたものが手元に残るのが普通の期待で、切って嬉しいのは通信量を切り詰めたいときだけ。
  キャッシュには期限と上限がかかっているので際限なく溜まることもない
  （切りたい人は `defaults write io.kkweb.gocci fetchWholeFile -bool NO`）
- 線引きは 32MB。Finder のサムネイルは大きい動画の先頭を数 MB 読むだけで、人が再生すれば軽く超える
- **残りが 32MB 以下なら、読まれた量に関わらず片付ける。** 小さい動画（10〜20MB）は
  Finder のサムネイルだけで9割方読まれる。そこで止めても得が無い

実機で確かめた（2026-08-13、キャッシュを空にしてから）:

| やったこと | 結果 |
| --- | --- |
| Finder で動画のフォルダを開く | 155MB。小さい動画をほぼ全部読んだぶんで、2.5GB のものは動かない |
| 2.5GB の動画を 60MB 読んで止める | そこから自動で進み、2分で 61% |

### キャッシュの寿命と上限（設定で変えられる）

rclone の既定は「最終アクセスから1時間、大きさの上限なし」。そのままだと、一度落ちてきた
ファイルが1時間で消え、バッジも雲に戻る。先読みを有効にすると、落としては消しを繰り返す。

**既定を 30 日・50GB にして、設定画面から変えられるようにした。** 空にすれば rclone の既定に
戻る。外付けが 500GB あり、そもそも内蔵の空きが乏しいから外付けに置いているので、
キャッシュに 50GB 使っても困らない。

## 手元の状態（2026-08-13 時点）

**アプリは完成に近い。常用できる状態で動いている。**

| 項目 | 状態 |
| --- | --- |
| マウント | `/Volumes/HIKSEMI/GoogleDrive`。ログイン時の自動マウントは未確認（ログイン項目が未登録） |
| キャッシュ | `/Volumes/HIKSEMI/.gocci-cache`。30日・50GB |
| Finder のバッジ | 出る。未ダウンロード／ダウンロード中／ダウンロード済み |
| 右クリック | 「ダウンロードを削除」 |
| 設定画面 | 7項目。マウント先・認証・キャッシュ・言語まわり |
| 試験 | `./test.sh`（計算）と `./test-live.sh`（実機7項目）が通る |
| 配布 | `release.sh` は書いてあるが、まだ一度も出していない |

書いたもの:

| ファイル | 中身 |
| --- | --- |
| `build.sh` | rclone と Sparkle を同梱し、拡張ごと署名する |
| `release.sh` | DMG と ZIP を作り、appcast に署名して GitHub Releases へ |
| `test.sh` / `test-live.sh` | 計算の試験と、実機を動かす試験 |
| `icon.sh` / `Tools/icon` | メニューバーの印の見本、アイコンの角の始末 |
| `Sources/Mount.swift` | rclone の起動・停止・見張り。今日の欠陥の多くはここ |
| `Sources/BadgeIndex.swift` | キャッシュを読んで、道ごとの取得状況を出す |
| `Sources/Finisher.swift` | 中断された取得の続き |
| `Sources/Evict.swift` | 「ダウンロードを削除」の実行役 |
| `Sources/FinderSync/` | Finder 拡張（バッジと右クリック） |
| `Sources/SettingsWindow.swift` | 設定画面 |
| `lp/` | LP（Next.js + next-intl）。**アプリの画面が未挿入** |

### アドホック署名では通れなかったもの

どれも Developer Program（年 99 USD）が要る。手元で実測した。

| やりたかったこと | 結果 |
| --- | --- |
| File Provider（dataless、標準の雲バッジ） | ドメインを登録できない |
| 通知センターで失敗を報せる | `Notifications are not allowed for this application` |
| 外付けの抜け殻を自分で外す | フルディスクアクセスが要り、更新のたびに切れる |

### 次にやること

1. **LP にアプリの画面を入れる。** 今の LP は文章とカードだけで、実物が1枚も無い
2. **常用して確かめる。** ログイン時の自動マウント（ログイン項目の登録が要る）、
   client_id の差し替え
3. **v1.0.0 を出す。** 数日使って問題が出なければ

### piro さんの手番

- 設定画面で「ログイン時に起動する」を入れる
- 自分の client_id と client_secret を入れて「保存して認証し直す」

### 参照する既存リポジトリ

同じ人が作った macOS メニューバーアプリ。作法はここに倣う。

| リポジトリ | 何を参照するか |
| --- | --- |
| `/Users/piro/Repository/konechi` | Swift の骨組み、`build.sh`、`release.sh`、Sparkle、SPEC.md の書式。一番近い |
| `/Users/piro/Repository/galopen` | アプリアイコンとトレイアイコンの作り、README の見せ方、LP |
| `/Users/piro/Repository/mac-classic-player` | アンマウント周りの扱い |

### 途中で止まったファイルを最後まで取りにいく

「開いたファイルを最後まで取得する」を入れているのに、47% で止まったまま残るのは
約束を守れていない。中断は普通に起きる（アプリの終了、外付けを抜く、通信の切断、
rclone の異常終了）ので、放っておくと途中のものが溜まる。

**設定が入っているときだけ、止まっているファイルを1本ずつ拾って続きを取りにいく。**

- **読ませるのは、欠けている最初の位置。** 先頭を読んでも、そこが既に手元にあれば
  rclone は何も取りに行かない。先読みは「読んだ位置より先」を取る仕掛けのため
- **時々つつく形では駄目。欠けている位置から読み進める。** 1 バイトでは何も起きず、
  1MB つつくと 30MB ほど取れて次まで止まる。36 秒止まって一気に増える動きになり、
  止まっているようにしか見えない。読み続ければ滑らかに進む
- **手元にある量は、範囲の重なりをまとめてから数える。** 足し込むと過大になる。
  実際は 291MB のものを 868MB と出していた
- **落ちてきている最中は手を出さない。** 次々つつくと、大きなファイルが何本も同時に
  落ち始めて回線を占有する
- 設定を切っている人には動かさない。開いてもいないファイルが裏で落ちてくることになる

### 設定画面

- **接続先（rclone の設定名）は、複数登録している人にだけ出す。** 1つなら選ぶ理由が無い。
  隠すのではなく行ごと置かない（隠すだけだと余白が残る）
- **選ぶ理由が無いものは出さない。** キャッシュの場所は外した。既定（マウント先と同じ
  ディスク）でほぼ正解になり、変える理由は例外的。必要なら
  `defaults write io.kkweb.gocci cacheDir <パス>` で変えられる
- **落とし穴は設定ではなく自動で避ける。** キャッシュ先が exFAT や FAT だと、rclone が使う
  穴あきファイルを作れず極端に遅くなる。その場合は内蔵へ逃がす
- **専門用語を出さない。** 「rcloneのリモート名」→「接続先」。値は rclone の設定から
  検出して選ばせる。利用者に名前を覚えさせない
- **自由入力を避ける。** 保存期間と上限は選択式（1日・1週間・30日・消さない／
  10GB・50GB・200GB・無制限）。`30d` `50G` のような書式を利用者に書かせない
- **説明文は、それが無いと困るものだけ。** 「実際に使ったファイルだけが対象です」のような
  内部の都合は書かない
- **入力欄・チェック・ボタン・説明文・版数を、同じ左端に揃える。** 見出しの幅を決めて、
  そのぶん空けたところに全部を置く

### 文言は Apple に合わせる

同じ性質の機能を持つ iCloud Drive の言い方を採る。独自の言い回しを作らない。

**訳語は推測しない。macOS 本体の翻訳表を引く。**

```bash
# 例: "Choose…" の日本語を調べる
python3 -c "
import plistlib
d = plistlib.load(open('/System/Library/Frameworks/AppKit.framework/Versions/C/Resources/Common.loctable','rb'))
print(d['ja']['Choose\u2026'])"   # → 選択…
```

`/System/Library/**/*.loctable` に Apple の訳が入っている。これで確かめた結果、
「選ぶ…」は誤りで「選択…」が正しかった。Mount / Unmount / Show in Finder / Quit は一致。

英語のボタンとメニューは**各単語の頭を大文字**にする（Check for Updates、How to Get One）。

| 変更前 | 変更後 |
| --- | --- |
| Finder で開く | Finderに表示 |
| 接続する / 接続を切る | マウント / マウント解除 |
| 接続中 | マウント済み（「接続中」は進行中とも読める） |
| 取得中 5 件 | 5項目をダウンロード中 |
| 手元から削除 | ダウンロードを削除 |
| 手元にある / クラウドのみ | ダウンロード済み / 未ダウンロード |

英数と日本語の間に空白を入れない（Apple の書き方）。

**「Finderを再起動」は、バッジが出ていないときだけ出す。** 他の項目がドライブへの操作なのに、
これだけアプリの不具合対処なので、普段は見せない。拡張が印を求めてきたかどうかで判断する。

### メニューは操作だけにする

場所・ダウンロード済みの量・取得中の一覧を出していたが、外した（2026-08-13）。
情報を並べるより、押せるものだけが並んでいる方が読みやすい。

出していたものは残してある（`BadgeIndex` が量と進捗を持っている）ので、必要になれば戻せる。

### 前のアプリが残した rclone を始末する

アプリが片付けの余地なく落ちると、子の rclone が生き残る。マウントを外してもプロセスは
残り、次のアプリが張り直すと同じ場所に複数の rclone がぶら下がる。互いに邪魔をして
取得が止まる。**実際に5つ溜まり、進捗が動かない原因になった。**

張り直す前に、同じマウント先を掴んでいる rclone を落とす。

このとき、原因の見立てを2回続けて外した。「メニューの項目は開いている間に描き直されない」
「止まっているものを表示していた」のどちらも違い、実際はプロセスの増殖だった。
**記録を取って測る前に結論を書かない。**

### 失敗に気付かせる

メニューバーの印は色が変わるだけで、開かないと気付けない。応答しないマウントに
1時間気付かなかったことがある。

**通知は使えない。** アドホック署名のアプリでは許可を求めた時点で
`Notifications are not allowed for this application` が返る（2026-08-13 に実測）。
File Provider と同じ壁で、Developer Program が要る。

**代わりに、失敗している間だけメニューバーの印を点滅させる。** 0.8 秒ごとに濃淡を変える。
割り込まないので作業を止めない。繋がれば止まる。

### 試験

- `./test.sh` — 計算の間違いを捕まえる。実機には触らない。共通の部品は `Sources/Paths.swift`
- `./test-live.sh` — 実機を動かす。検証用のマウント先を**起動引数で渡す**ので、
  保存された設定は変わらない（macOS の設定は `-キー 値` の起動引数で上書きできる）

実機の試験で見つけた欠陥:

- **`pkill`（SIGTERM）で終了させるとアンマウントされない。** 既定では即座に落ちるので、
  終了時の片付けが走らない。合図を受け取って正規の終了へ回すようにした
- **抜け殻を外した直後に張り直すと `Resource busy` で撥ねられる。** 外れた後も場所が
  しばらく掴まれている。外してから少し置き、それでも撥ねられたら数回だけやり直す

### 次にやること

1. **Finder Sync 拡張の試作。** まず「アドホック署名の拡張を有効にできるか」の1点だけ確かめる
2. **キャッシュの寿命と上限。** バッジを意味のあるものにするために要る
3. **LP。** `lp/` に Next.js + next-intl で。`gocci.kkweb.io`
4. **最初のリリース。** 1週間ほど常用して、問題が出なければ v1.0.0 を出す

### Finder に実体の有無を出す（入れた）

CloudMounter で見えていた「このファイルは手元にあるか」を、Gocci でも出す。

**Finder Sync 拡張**（`com.apple.FinderSync`）で実現した。CloudMounter も同じ手で、
`CMFinderSync.appex` を同梱している。外付けの上に自前で NFS マウントを張ったまま
バッジを出していたので、**マウント先を選べることと両立する**。File Provider は要らない。

決めたこと:

- **拡張はサンドボックスが必須。** `com.apple.security.app-sandbox` を付けずに署名すると、
  そもそも `pluginkit` の一覧に載らず、システム設定から有効にできない。付けた途端に載った
- **設定の受け渡しに App Group は使わない。** 団体識別子が要るので、アドホック署名では作れない。
  代わりに**アプリが拡張のコンテナへ直接書く**。アプリはサンドボックスの外にいるので書けるし、
  拡張は自分のコンテナなら読める
- **判定は rclone の控え（`vfsMeta`）を読む。** そこに本来の大きさ（Size）と、落ちてきた
  範囲（Rs）が入っている。実体の有無だけを見ると、2% しか落ちていないものまで
  「手元にある」と数えることになる（実際にそう見えて、作り直した）
- **覗かれただけのものは雲にする。** Finder はサムネイルのために中を読むので、
  数十 MB のファイルだと 10% を超えて「取得中」に見える。触っていないのに進行中に
  見えるのは筋が悪い。線引きはアプリが「使った」と見なす量（32MB）に揃える。
  メニューの「取得中」には本当の値を出す。**バッジは丸め、メニューは正確に。**
- **バッジは10きざみ。** 0% は雲、10〜90% は青い円グラフ、100% は緑のチェック。
  Finder のバッジは絵の差し替えなので、段の数だけ絵を持たせる。なめらかには動かない。
  切り上げない。ほとんど落ちていないものを「取得中」とは見せない
- **同じ名前のファイルが並ぶと、片方にしか印が出ない。** Drive は同じフォルダに同名を置けるが、
  rclone のキャッシュは道で管理しているので区別できない。こちらでは直せない
- **フォルダには付けない。** 実体という話が当てはまらない
- **一度描いた印は Finder から訊き直されない。** 一覧が変わったら拡張の側から塗り直す。
  反映は最長8秒（アプリが5秒ごとに書き、拡張が3秒ごとに読む）
- **バッジの枠は Finder が決める。** アプリから大きさは変えられないので、できるのは
  枠いっぱいまで描くこと。倍の解像度（64px）で作る。
  SF Symbols の絵は外周に1割ほどの余白を持つ（実測: `cloud.fill` は 87x63 の枠に中身 78x52）。
  色が乗っている範囲を測って切り出し、そこを枠に合わせる。これで 8x8 → 10x10 になった

  実測した大きさ（2026-08-13）:

  | 表示 | バッジ |
  | --- | --- |
  | リスト表示（アイコン小・大とも） | 10x10 |
  | アイコン表示 | 20x20 |

  白い縁を付けて浮かせる案は、10 画素では潰れて見分けが付かなかった（取り消した）
- **拡張を入れ替えたら Finder の再起動が要る。** `killall Finder`。
  これは利用者にも起きる。Sparkle で更新するたびに拡張が入れ替わり、Finder を
  再起動するまでバッジが黙って出なくなる。**メニューに「Finder を再起動」を置いた。**
  勝手には再起動しない。開いているウィンドウが閉じるため
- **見張る場所（`directoryURLs`）を入れ直すのは、行き先が変わったときだけ。** 一覧が変わる
  たびに入れ直すと、そのつど Finder 側の登録がやり直しになり、印を訊きに来なくなる。
  先読みを入れて割合が数秒ごとに動くようになって発覚した

### 手元から削除

一度落ちてきたファイルをクラウドのみに戻す手段が無かった。寿命か上限で自然に消えるのを
待つしかなく、rclone にもキャッシュを捨てる命令が無い（`vfs/forget` はフォルダ一覧の話）。

**Finder の右クリックに「手元から削除」を置いた。** 拡張はサンドボックスの中にいて
キャッシュへ手が届かないので、頼みごとを自分のコンテナへ書き、アプリが拾って消す。
バッジの一覧と同じ経路を逆向きに使っている。

- **消すのは rclone のキャッシュだけ。** Drive の実体には触らない
- **書き戻していないものは消さない。** 消せば書いた内容が失われる。`vfsMeta` の `Dirty` を見る
- キャッシュのファイルを直に消して差し支えないことは確かめた。rclone は控えを作り直し、
  次に読んだときに落とし直す

**マウントの中のファイルをゴミ箱に入れると Drive の実体が消える。** 手元から追い出す操作とは
別物なので、README に書く。

### マウント表に載っていても、生きているとは限らない

`statfs` は抜け殻でも「マウント済み」と答える。ここを信じると、繋がっているつもりのまま
Finder だけが永久に待つ。実際に1時間待たされた（2026-08-12）。

起きたことの順序:

1. アプリを入れ替えたとき、前のアプリの rclone が親を失って残った
2. 新しいアプリがそのマウントを引き継いだ。**自分の子ではないので、死んでも終了が届かない**
3. その rclone が死んだ。アプリは繋がっているつもりのまま
4. Finder が固まった

決めたこと:

- **自分が起動していないマウントは引き継がない。** 外して張り直し、必ず自分の子として持つ。
  引き継ぐと見張りが利かなくなる
- **1分ごとに読み出しを試す。** 返らなければ落ちたものとして扱う。息はあるのに返事をしない
  rclone はこちらで落とす。生かしたままでは場所を握られたままになる
- 起動時に状態を決め打ちすることもやめた。決めるのはマウントの手続きの中だけ

### 外付けに置く（2026-08-17 に通した）

**File Provider でも外付けに置ける。** 置き場所は設定で選び、選ばなければ内蔵に置く。
`NSFileProviderDomain(displayName:userInfo:volumeURL:)` でボリュームを指す（macOS 15 以降）。

要ったのは次の4つ。どれもヘッダには書かれておらず、実物を見るか動かすまで分からなかった。

**1. 拡張の Info.plist に `NSExtensionFileProviderAllowsExternalVolumes` を宣言する。**
無いと `volumeURL` 付きの登録が `3328 feature is not supported` で断られる。
このキーは SDK のヘッダに出てこない。CloudMounter の `MountProvider.appex` の
Info.plist を読んで見つけた。

**2. ドメインを rclone より先に作る。**
rclone を起こしてから申し出ると `513` で断られ続ける。順序を入れ替えるだけで通った。
理由は分かっていない。

**3. 外したドメインが残す記録を片付ける。**
`\(ボリューム)/.CloudStorage/System/io.kkweb.gocci.FileProvider` が残り、
残ったままだと次の登録が `513` になる。消せばその場で作れた。

**4. 渡すファイルを、ドメインと同じボリュームに置く。**
`NSFileProviderReplicatedExtension.h` 1240行に「利用者から見える URL と同じボリュームの
通常ファイルであること」と書いてある。手元の一時置き場に作ると、内蔵では偶然通るが、
外付けでは同じ範囲を延々と要求され、1バイトも読み進めない。
置き場所は `NSFileProviderManager.temporaryDirectoryURL()` に訊く。
外付けの識別子は毎回変わるので、拡張が `init(domain:)` で受け取った domain を控えておく。

**これは途中を渡す道と丸ごと渡す道の両方に要る。** 片方（`fetchPartialContents`）だけ直して
`fetchContents` を手元の一時置き場のままにしていたため、丸ごと落とすと Finder が
「ファイルまたはディレクトリがありません」を出した。こちらのログは「渡した」で終わっており、
断られているのは受け取る側なので、`fileproviderd` を見ないと分からない（2026-08-17 実測）。

```text
GocciFileProvider: 渡した: Brand New Day.mp3
fileproviderd:     Download root <private> failed with error: POSIX 2
```

**さらに、そこへ書けるのは拡張だけ。** 渡し先を `temporaryDirectoryURL()` にしただけでは
まだ通らない。丸ごと落とすのに `operations/copyfile` を使うと、書きに行くのは rclone、
つまり別のプロセスになる。`.CloudStorage` の下は macOS が守っていて、`ls -lde` では
755 のただのフォルダに見えるのに `operation not permitted` で弾かれる。
いったん外へ落としてから移す手も駄目で、今度は移すところで断られた。
**丸ごと落とす道も、拡張が自分で `serve` の口から流し込んで書く**（どちらも 2026-08-17 実測）。

```text
rclone:            Failed to copy: open …/.CloudStorage/System/…/tmp/…/x.mp3.partial:
                     operation not permitted
GocciFileProvider: 渡す場所へ移せなかった: “…”へのアクセス権がないため
```

確かめるときは Finder の右クリックの「今すぐダウンロード」を押す。読むだけだと
`fetchPartialContents` の側しか通らず、丸ごとの道は試されない。
なお失敗が残っていると Finder に「デバイスはサーバに接続できませんでした」の板が出たまま
になり、以降の操作が `-15260 Finder はビジー状態です` で通らなくなる。先に閉じる。

**5. 途中を渡すときは、頼まれた分より多く取っておく。**
macOS が訊いてくるのは 16KB ずつで、一度取るたびに間を置かれる。頼まれた分ちょうどを
返すと 9MB のファイルに18分かかった。8MB ずつ先を取るようにして3秒になった
（2026-08-17 実測。どちらも MD5 は Drive 側と一致）。

#### 遠回りした原因（同じ轍を踏まないために）

- **`/Applications` に古いビルドが入ったまま2時間以上検証した。** 「順序が原因」「Sparkle が
  上書きしている」など、この間の結論は全部無効だった。ビルドしたものと動いているものの
  ハッシュを毎回突き合わせる
- **`diskutil enableOwnership` を根拠なく提案した。** 外付けの根が書けなくなり、`sudo` でも
  所有者を変えられない状態に陥った。抜き差しで戻る。所有権は `513` の原因ではなかった
- **`pluginkit -r` で拡張の登録を外した。** ドメインが「利用者が無効にした」扱い（`-2011
  NSFileProviderErrorDomainDisabled`）になり、システム設定で入れ直すまで戻らない。
  ドメインの有効・無効はアプリからは変えられない（`NSFileProviderDomain.h` 183行）
- **`-2011` を拡張側のエラーだと読み違えた。** これはシステムが出すもので、
  ゴミ箱の作成や入れ物の作成が断られたときにも同じ番号が出る

## 調べた結果、採らなかったもの

- **Google 公式のドライブアプリ** — マウント先を選べず、内蔵ディスク固定。内蔵の空きが 42GB しか無い
- **Mountain Duck** — macOS で使えるのは Integrated モードだけで、ドキュメントに
  「Custom mount location is not honoured in Integrated connect mode but always in
  `~/Library/CloudStorage`」と明記がある。マウント先もキャッシュ先も内蔵固定
- **CloudMounter を使い続ける** — 表示を絞る設定が無い。アプリの文言リソースを全部確認した結果、
  あるのはマウント種別、ネットワークドライブ化、他アカウントへの表示、キャッシュフォルダ・寿命・
  更新間隔、デスクトップ表示、サイドバー名、ドラッグ&ドロップ時のコピー、暗号化だけだった
